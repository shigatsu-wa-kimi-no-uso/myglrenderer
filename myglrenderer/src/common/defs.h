#pragma once
#ifndef DEFS_H
#define DEFS_H

using uint = unsigned int;
using uchar = unsigned char;
using byte = unsigned char;
using word = unsigned short;
using dword = unsigned long;
using qword = unsigned long long;
using ulonglong = qword;


#endif

